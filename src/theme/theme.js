import getMuiTheme from 'material-ui/styles/getMuiTheme'

const muiTheme = getMuiTheme({
  palette: {
    primary1Color: '#EEFF41'
  },
  textField: {
    textColor: '#ffffff',
    hintColor: '#c2c2c2'
  }
})

export default muiTheme
